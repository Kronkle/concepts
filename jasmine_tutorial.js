// Jasmine is a BDD testing framework for JS that runs independently of any browsers, JS framework, and the DOM

// This is a Jasmine source file, which contains defintions for functions that will be tested by the associated Jasmine spec file
var n = 3;
var m = 4;

function addSomething(n, m) {
	return n + m;
};

function stateName() {
	return {
		name: "Mike"
	};
};
